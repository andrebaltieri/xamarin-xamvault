<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="XamVault.Views.CreatePasswordPage"
    Title="New password">
    <ContentPage.ToolbarItems>
        <ToolbarItem            
            Text="Refresh"
            Command="{Binding NavigateToCreatePasswordPageCommand}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout
            Padding="12">
            <Entry Placeholder="Title or description" />
            <Entry
                BackgroundColor="Transparent"                
                FontSize="22"
                Text="C%WHP6$#23^;"
                CharacterSpacing="8"
                HeightRequest="60"
                HorizontalTextAlignment="Center" />
            <Button
                BackgroundColor="Transparent"
                TextColor="Black"
                Text="Copy to clipboard" />
            <BoxView HeightRequest="20" />
            <Button Text="Save" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
